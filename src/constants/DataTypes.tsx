export interface ValueType {
    volume: number;
    currentSong: string;
    analyticsOn: boolean;
    isPlaying: boolean;
}

export interface SongType {
    title: string;
    author: string;
    isDownload: boolean;
}